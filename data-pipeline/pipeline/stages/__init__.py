"""Bake stages (design section 11a): ``cases`` builds a case's Contract 2 artifact and ``benchmark``
assembles the benchmark from one run's records; ``preprocess`` and ``particle_experiment`` belong to
the HZDR particle lane (``data-pipeline/run_particles.py``)."""
