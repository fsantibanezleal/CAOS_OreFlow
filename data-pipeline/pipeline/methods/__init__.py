"""Methods that read the process engine: constrained optimization, uncertainty and sensitivity, and the
learned lane. Each measures its own error or feasibility against the engine it approximates. Numeric
settings are declared in ``engine/data/constants.json`` with unit and source, like the engine's own.
"""
