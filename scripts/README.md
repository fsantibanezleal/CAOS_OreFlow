# OreFlow scripts

`setup` creates ignored `.venv` and `.venv-gpu` environments. `fetch-data` downloads and verifies the HZDR workbook. `precompute` runs the complete scientific bake. `smoke` runs a sandbox bake plus artifact, residue and content guards. `dev` starts the Vite workbench.
