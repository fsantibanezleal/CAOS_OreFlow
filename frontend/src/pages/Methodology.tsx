import { Link } from 'react-router';
import { PageHeading, ResearchSection, Tabset } from '../content/Research';
const p = (en: string, es: string) => [en, es] as const;
export default function Methodology() {
  const common = (title: readonly [string, string], refs: string[], paragraphs: Array<readonly [string, string]>, equations: Array<{ tex: string; caption: readonly [string, string] }>, diagram?: string) => <ResearchSection title={title} refs={refs} paragraphs={paragraphs} equations={equations} diagram={diagram}/>;
  return <div className="of-page of-content"><PageHeading title={p('A model stack you can interrogate', 'Una pila de modelos interrogable')} lede={p('The methodology moves from particle-size laws to circuit-level decisions and then asks what a learned approximation can safely inherit.', 'La metodología va desde leyes de tamaño de partícula a decisiones de circuito y luego pregunta qué puede heredar con seguridad una aproximación aprendida.')}/><Tabset tabs={[
    { id: 'energy', label: p('Energy laws', 'Leyes de energía'), content: common(p('1. Three comminution laws, three questions', '1. Tres leyes de conminución, tres preguntas'), ['bond'], [p('Rittinger emphasises new surface area and becomes highly sensitive as product size shrinks. Kick treats the reduction ratio geometrically and is more appropriate for coarse breakage. Bond places the work index between feed and product sizes and remains the engineering reference used in the baked energy ledger.', 'Rittinger enfatiza nueva superficie y se vuelve sensible al reducir tamaño. Kick trata la razón geométricamente y es útil para rotura gruesa. Bond ubica work index entre tamaños y sigue como referencia de ingeniería en ledger.'), p('OreFlow evaluates all three at the same P80. This is an intentional comparison of scaling assumptions, not three calibrated estimates of one mill. Their disagreement is the point: it identifies where product-size decisions are doing most of the work.', 'OreFlow evalúa las tres en el mismo P80. Es comparación de escalas, no tres estimaciones calibradas de un molino. Su desacuerdo es el punto: identifica dónde decisión de tamaño domina.'), p('The Bond work index is case-specific in the registry, while Rittinger and Kick coefficients are declared global research constants. A real calibration would replace those values with laboratory work, ore competency and equipment efficiency factors.', 'Bond work index es específico de caso, mientras coeficientes Rittinger y Kick son constantes declaradas. Calibración real reemplazaría valores con trabajo de laboratorio, competencia y eficiencia.'), p('All energy numbers are specific energy proxies. They do not include motor efficiency, liner wear, circulating load, media, capital or power price. They are suitable for a visual sensitivity question, not a feasibility study.', 'Todos los números son proxies de energía específica. No incluyen eficiencia, desgaste, carga circulante, medios, capital ni precio. Sirven para sensibilidad visual, no estudio de factibilidad.')], [{ tex: String.raw`E_R=C_R\left(\frac{1}{P_{80}}-\frac{1}{F_{80}}\right)`, caption: p('Rittinger increases with surface area created.', 'Rittinger aumenta con superficie creada.') }, { tex: String.raw`E_B=10W_i\left(\frac{1}{\sqrt{P_{80}}}-\frac{1}{\sqrt{F_{80}}}\right)`, caption: p('Bond work-index form used in the ledger.', 'Forma Bond usada en ledger.') }], 'pipeline') },
    { id: 'grinding', label: p('Grinding PBM', 'PBM de molienda'), content: common(p('2. A population balance keeps the distribution alive', '2. Un balance poblacional mantiene la distribución'), ['pbm', 'bond'], [p('A single P80 hides the tail that classification and flotation later see. The process model therefore carries a cumulative particle-size response over a logarithmic grid. Its slope is softened by hardness and crusher reduction, making the tail a visible state variable.', 'Un P80 único oculta la cola que luego ven clasificación y flotación. El modelo lleva respuesta acumulativa sobre grilla logarítmica. Pendiente se suaviza por dureza y reducción de trituradora, haciendo visible la cola.'), p('This compact PBM proxy is not a full discretised breakage kernel with residence-time distribution. It is a structured approximation selected so the user can alter the grind target and see the entire distribution respond in a bounded interaction.', 'Este proxy PBM compacto no es kernel discretizado completo con distribución de residencia. Es aproximación estructurada para alterar target y ver distribución responder en interacción acotada.'), p('The crusher stage uses CSS and feed P80 to produce a product P80. That output feeds the grinding response, so a change upstream is not overwritten by an independent downstream target.', 'Trituradora usa CSS y feed P80 para producir P80. Ese resultado alimenta molienda, por lo que cambio upstream no se sobrescribe con target independiente.'), p('The resulting arrays are stored in every variant artifact and plotted with hover values. This is more informative than a single “fineness” KPI because it reveals where curves cross and where model conclusions are tail-driven.', 'Arreglos resultantes quedan en cada artefacto y se grafican con hover. Es más informativo que KPI único porque revela cruces y conclusiones dominadas por cola.')], [{ tex: String.raw`\frac{\partial M_i}{\partial t}=S_{i+1}M_{i+1}-S_iM_i+\sum_{j>i}b_{ij}S_jM_j`, caption: p('General population-balance form; OreFlow uses a compact cumulative proxy.', 'Forma general de balance; OreFlow usa proxy acumulativo compacto.') }], 'instrument') },
    { id: 'classification', label: p('Classification', 'Clasificación'), content: common(p('3. Partition curves turn size into probability', '3. Curvas de partición vuelven tamaño probabilidad'), ['hydrocyclone'], [p('A classifier does not split a sample at one perfect aperture. Real separation is probabilistic. OreFlow uses a logistic partition to expose the d50 and imperfection, then applies a Plitt-style cut-size approximation affected by water, density and feed load.', 'Un clasificador no divide muestra en apertura perfecta. Separación real es probabilística. OreFlow usa partición logística para exponer d50 e imperfección, y aproximación estilo Plitt afectada por agua, densidad y carga.'), p('The overflow curve is the ground distribution multiplied by the fine-recovery probability. It is therefore possible for a finer grind to increase fines while a poor classifier still sends them away. This coupling is why classification deserves its own tab and metric.', 'Curva overflow es distribución molida por probabilidad de finos. Molienda fina puede aumentar finos mientras mal clasificador los pierde. Este acoplamiento justifica tab y métrica propios.'), p('Water is included as a declared process variable, not a visual slider detached from physics. Increasing water shifts the modeled cut response and changes plant water demand through throughput times water per tonne.', 'Agua es variable declarada, no slider separado de física. Aumentar agua desplaza corte modelado y cambia demanda por throughput por agua por tonelada.'), p('The classifier model is a research-level approximation. It does not represent cyclone geometry, apex, vortex finder, pressure drop or rheology in enough detail to authorize equipment selection.', 'Modelo de clasificador es aproximación de investigación. No representa geometría, apex, vortex finder, caída de presión o reología con detalle para seleccionar equipo.')], [{ tex: String.raw`P_{\mathrm{overflow}}(d)=\frac{1}{1+\exp\left((d-d_{50})/(\alpha d_{50})\right)}`, caption: p('Logistic fine-recovery partition used for the response curve.', 'Partición logística de finos usada en curva.') }], 'data') },
    { id: 'flotation', label: p('Flotation kinetics', 'Cinética de flotación'), content: common(p('4. Residence time reveals kinetic populations', '4. Tiempo de residencia revela poblaciones cinéticas'), ['flotation'], [p('The first-order form treats the valuable fraction as one kinetic population. Kelsall separates fast and slow populations, while the compressed exponential changes the shape of the approach to a plateau. Comparing them shows how the same final time can imply a very different early recovery path.', 'Forma de primer orden trata fracción valiosa como una población. Kelsall separa rápida y lenta, mientras exponencial comprimida cambia forma al plateau. Compararlas muestra trayectorias diferentes.'), p('Reagent dose and air rate enter the rate constant through saturating response terms. Those saturations are a useful reminder that dosage is not linearly exchangeable with time, and that increasing one operating variable can leave another bottleneck unchanged.', 'Dosis y aire entran en tasa con términos saturantes. Recordatorio: dosis no es intercambiable linealmente con tiempo y variable puede dejar otro cuello de botella.'), p('The circuit converts recovery into concentrate grade using a mass-pull proxy and closes valuable metal balance analytically. The balance is exact for the declared one-stage model; it is not evidence that a plant has no sampling or accounting error.', 'Circuito convierte recuperación a ley con proxy mass-pull y cierra balance analíticamente. Balance exacto para modelo declarado, no evidencia de planta sin error de muestreo.'), p('The visual curve is a hypothesis generator. Metallurgical testing would fit rate populations, entrainment, froth stability and reagent selectivity to observed time-recovery data before any production decision.', 'Curva visual genera hipótesis. Prueba metalúrgica ajustaría poblaciones, arrastre, estabilidad y selectividad con datos observados antes de decisión productiva.')], [{ tex: String.raw`R(t)=R_{\max}\left(1-e^{-kt}\right)`, caption: p('First-order cumulative recovery.', 'Recuperación acumulativa de primer orden.') }, { tex: String.raw`R_K(t)=R_{\max}\left[f(1-e^{-k_ft})+(1-f)(1-e^{-k_st})\right]`, caption: p('Kelsall fast and slow populations.', 'Poblaciones rápida y lenta de Kelsall.') }], 'lanes') },
    { id: 'optimization', label: p('Optimisation', 'Optimización'), content: common(p('5. Constrained optimisation is a transparent search', '5. Optimización acotada es búsqueda transparente'), ['scipy-opt'], [p('OreFlow’s optimizer enumerates bounded grind and reagent factors and evaluates the same circuit used by the workbench. The candidate objective rewards recovery and concentrate grade while penalising specific energy and reagent dose.', 'Optimizador enumera factores acotados de molienda y reactivo y evalúa mismo circuito. Objetivo premia recuperación y ley y penaliza energía y dosis.'), p('The implementation records the winning objective and operating point, not just a success flag. A researcher can therefore compare whether the “best” point comes from a real recovery gain, an energy penalty or the arbitrary weight choices.', 'Implementación registra objetivo y punto ganador. Investigador puede comparar si “mejor” viene de recuperación, penalización energética o pesos elegidos.'), p('Constraints include the physical ordering of product sizes and mass-balance closure. The search is not a global nonlinear optimum because the feasible region, weights and model forms are intentionally limited.', 'Restricciones incluyen orden físico de tamaños y cierre de balance. No es óptimo global porque región, pesos y formas están limitados.'), p('For plant deployment, this layer should be connected to measured economics, equipment envelopes, water permits and operator constraints. OreFlow exposes where those inputs would enter without inventing them.', 'Para planta, esta capa debe conectarse a economía medida, equipos, permisos de agua y operadores. OreFlow muestra dónde entrarían sin inventarlos.')], [{ tex: String.raw`\max_{x\in\Omega}\;J(x),\quad J=R_c+0.35G_c-0.18E-0.006D`, caption: p('Declared research objective in the 25-point grid search.', 'Objetivo declarado en la búsqueda de 25 puntos.') }], 'release') },
    { id: 'learning', label: p('Learning and OOD', 'Aprendizaje y OOD'), content: common(p('6. Learned models inherit the simulator boundary', '6. Modelos aprendidos heredan límite del simulador'), ['ml-mining', 'ml-review', 'sklearn', 'pytorch'], [p('The feature matrix spans 720 seeded parametric variants around the twelve authored cases. The target is rougher recovery produced by the declared circuit, so the learned models are surrogate models of a known simulator, not discoveries from plant observations.', 'Matriz abarca 720 variantes paramétricas sembradas alrededor de doce casos. Target es recuperación rougher del circuito declarado, por lo que modelos son surrogate de simulador, no descubrimientos de observaciones.'), p('Ridge establishes a smooth baseline, random forest and gradient boosting model nonlinear interactions, and Gaussian process gives a local uncertainty estimate. The MLP is trained with PyTorch and exported to ONNX when the accelerator environment has the required exporter.', 'Ridge establece base suave, random forest y gradient boosting modelan interacciones y Gaussian process da incertidumbre local. MLP se entrena con PyTorch y exporta ONNX cuando entorno tiene exporter.'), p('An autoencoder reconstruction error is stored as a diagnostic, not a probability of correctness. High reconstruction error means the feature vector looks unlike training vectors; it does not prove the process is unsafe or wrong.', 'Error de reconstrucción de autoencoder es diagnóstico, no probabilidad. Error alto significa vector distinto a entrenamiento; no prueba que proceso sea inseguro o erróneo.'), p('The evaluation split is disjoint from training and reports percentage-point RMSE. The benchmark page keeps the test protocol visible so a visually convincing model cannot hide a weak or out-of-domain result.', 'Partición de evaluación es disjunta y reporta RMSE en puntos porcentuales. Benchmark mantiene protocolo visible para que modelo convincente no oculte resultado débil o fuera de dominio.')], [{ tex: String.raw`\hat{y}=f_\theta(x),\quad \mathrm{RMSE}=\sqrt{\frac{1}{n}\sum_i(y_i-\hat{y}_i)^2}`, caption: p('Prediction and held-out error reported by the pipeline.', 'Predicción y error reservado reportados por pipeline.') }], 'lanes') },
  ]}/><div className="of-next"><Link to="/implementation">Open implementation details ↗</Link></div></div>;
}
