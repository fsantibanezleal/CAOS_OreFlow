import { Link } from 'react-router';
import { PageHeading, ResearchSection, Tabset } from '../content/Research';
const p = (en: string, es: string) => [en, es] as const;
const section = (title: readonly [string, string], refs: string[], text: readonly [string, string], diagram?: string) => <ResearchSection title={title} refs={refs} diagram={diagram} paragraphs={[text, p('Each experiment is an evidence slice, not a universal conclusion. The protocol, seed, partition or perturbation is named so a future calibration can replace the authored value without changing the interpretation boundary.', 'Cada experimento es una porción de evidencia, no conclusión universal. Protocolo, semilla, partición o perturbación se nombran para que calibración futura reemplace valor sin cambiar límite.'), p('The workbench is designed to let a researcher move from the figure to the exact case, variant, method and parameter record. That relationship is important: visual insight without a reproducible input is only a suggestion.', 'El laboratorio permite pasar de figura a caso, variante, método y parámetros exactos. La relación es importante: intuición visual sin entrada reproducible es solo sugerencia.'), p('Results below are interpreted at the level supported by the data. A synthetic response can test implementation and provoke a hypothesis. It cannot establish a plant-wide operating envelope or transfer guarantee.', 'Resultados se interpretan al nivel soportado por datos. Respuesta sintética prueba implementación y provoca hipótesis. No establece envelope de planta ni garantía de transferencia.')]}/>;
export default function Experiments() {
  return <div className="of-page of-content"><PageHeading title={p('Experiments with a declared boundary', 'Experimentos con límite declarado')} lede={p('The experiments page is where OreFlow keeps its claims small enough to be falsifiable and rich enough to guide the next test.', 'La página de experimentos mantiene afirmaciones falsables y suficientemente ricas para guiar la próxima prueba.')}/><Tabset tabs={[
    { id: 'coverage', label: p('Coverage matrix', 'Matriz de cobertura'), content: section(p('1. Complete method coverage', '1. Cobertura completa de métodos'), ['sklearn', 'ml-mining'], p('The bake contains 12 process cases, six variants per case and 19 method outputs per variant. The matrix is not a count inferred from the UI: it is generated from the artifacts and checked by scripts/check_artifacts.py. Classical, optimisation, uncertainty, learned and frontier tiers all appear in the same ledger.', 'La construcción contiene 12 casos, seis variantes por caso y 19 salidas por variante. Matriz no es conteo inferido de UI: se genera de artefactos y se verifica por script. Capas clásica, optimización, incertidumbre, learned y frontier aparecen en ledger.'), 'pipeline') },
    { id: 'size', label: p('Grind ablation', 'Ablación de molienda'), content: section(p('2. The grind-size ablation', '2. Ablación del tamaño de molienda'), ['bond', 'pbm'], p('The fine-feed and coarse-grind variants perturb P80 while preserving the rest of the case identity. The expected pattern is a lower product P80, higher surface-area energy and a different kinetic response. The artifact records the complete curves so a reader can verify whether those patterns actually occur.', 'Variantes fine-feed y coarse-grind perturban P80 conservando identidad. Patrón esperado: P80 menor, energía de superficie mayor y cinética diferente. Artefacto registra curvas para verificar si ocurre.'), 'instrument') },
    { id: 'kinetics', label: p('Kinetic ablation', 'Ablación cinética'), content: section(p('3. Fast versus slow flotation populations', '3. Poblaciones rápidas y lentas'), ['flotation'], p('First-order, Kelsall and compressed-exponential methods are evaluated at the same operating point. Their end points are deliberately close enough to compare shape, not just score. A fast initial response and a long slow tail imply different value in residence time, so one recovery number is not enough.', 'Primer orden, Kelsall y exponencial se evalúan en mismo punto. Endpoints cercanos permiten comparar forma, no solo score. Respuesta rápida y cola lenta implican valor distinto de residencia, por lo que un número no basta.'), 'lanes') },
    { id: 'uncertainty', label: p('Robust uncertainty', 'Incertidumbre robusta'), content: section(p('4. Declared uncertainty, solved repeatedly', '4. Incertidumbre declarada, resuelta repetidamente'), ['hzdr', 'scipy-opt'], p('The uncertainty experiment samples positive multipliers for hardness, head grade and cyclone cut using a seeded lognormal distribution. Each draw re-enters the circuit and the recovery quantiles are retained. This is conditional uncertainty around an authored state, not a calibrated confidence interval for a mine.', 'Experimento muestrea multiplicadores positivos de dureza, ley y corte con lognormal sembrada. Cada muestra vuelve al circuito y conserva cuantiles. Es incertidumbre condicional de escenario, no intervalo calibrado de mina.'), 'data') },
    { id: 'leakage', label: p('Leakage audit', 'Auditoría de fuga'), content: section(p('5. Learning without label leakage', '5. Aprendizaje sin fuga de etiquetas'), ['sklearn', 'ml-review'], p('The feature design is shuffled once, with training, validation and test counts stored in the artifact. The evaluation uses disjoint perturbations built after the training matrix. Model selection and reporting therefore have an auditable split, although the target remains simulator-generated.', 'Diseño de features se mezcla una vez y guarda conteos. Evaluación usa perturbaciones disjuntas creadas después de matriz de train. Split es auditable, aunque target sigue generado por simulador.'), 'pipeline') },
    { id: 'novel', label: p('Novel demonstration', 'Demostración novedosa'), content: section(p('6. A proposed research demonstration: uncertainty-aware digital twin', '6. Propuesta: digital twin consciente de incertidumbre'), ['ml-mining', 'pytorch', 'onnx'], p('The novel demonstration is a vertical comparison: particle descriptors inform an explicit feature contract, the calibrated circuit generates process labels, and a surrogate reports both prediction and out-of-domain reconstruction error while the user drags P80, water and reagent. The key result is not a leaderboard. It is whether an uncertainty-aware, conservation-checked surrogate can preserve the same decision ranking as the mechanistic circuit across held-out ore regimes.', 'La demostración novedosa compara verticalmente: descriptores de partículas informan contrato, circuito calibrado genera labels y surrogate reporta predicción y error OOD al mover P80, agua y reactivo. Resultado no es leaderboard, sino preservar ranking con conservación en regímenes reservados.'), 'lanes') },
  ]}/><div className="of-callout-novel"><strong>Novelty status</strong><p>Implemented as a reproducible demonstration scaffold with actual artifacts and model tiers. A publishable scientific claim still requires measured plant labels, calibration and cross-site holdout.</p><Link to="/benchmark">See the measured artifact ledger ↗</Link></div></div>;
}
