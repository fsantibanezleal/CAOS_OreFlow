import type { ArchitectureConfig } from '@fasl-work/caos-app-shell';
export const ARCHITECTURE: ArchitectureConfig = { title_en: 'How OreFlow works', title_es: 'Cómo funciona OreFlow', tabs: [
  { id: 'instrument', en: 'The instrument', es: 'El instrumento', svg: `${import.meta.env.BASE_URL}svg/tech/instrument.svg`, body_en: 'OreFlow is an integrated process workbench. The input contract describes feed, comminution, classification and flotation operating points. The six routes separate the instrument from the explanation and evidence. A selected case and variant identify the exact state behind every chart and table. The workbench can use reviewed replay artifacts, a bounded local analytical engine, or a live API response when that lane is configured.', body_es: 'OreFlow es un laboratorio integrado de procesos. El contrato describe alimentación, conminución, clasificación y flotación. Las seis rutas separan instrumento, explicación y evidencia. Caso y variante identifican el estado exacto detrás de cada gráfico y tabla. El laboratorio usa artefactos revisados, motor analítico local acotado o API en vivo cuando esa vía está configurada.' },
  { id: 'pipeline', en: 'Scientific pipeline', es: 'Proceso científico', svg: `${import.meta.env.BASE_URL}svg/tech/pipeline.svg`, body_en: 'The canonical offline path is ingest, preprocess, dataset, feature extraction, train, inference, evaluation, export and validation. The raw HZDR workbook remains ignored; its aggregate summary and DOI are committed. Authored process cases are labelled as scenarios. Models are trained on a disjoint parametric design and evaluated on a held-out perturbation set, with model files and registries preserved.', body_es: 'La vía local es ingestión, preproceso, conjunto, características, entrenamiento, inferencia, evaluación, exportación y validación. El libro HZDR queda ignorado; resumen y DOI sí se versionan. Casos son escenarios declarados. Modelos se entrenan con diseño paramétrico separado y se evalúan en perturbaciones reservadas, conservando modelos y registros.' },
  { id: 'lanes', en: 'Execution lanes', es: 'Vías de ejecución', svg: `${import.meta.env.BASE_URL}svg/tech/lanes.svg`, body_en: 'Offline GPU or CPU baking creates canonical evidence. The browser replay lane reads only committed JSON. The live lane recomputes a bounded response in TypeScript for controls and animation. It is a visibly distinct approximation and never rewrites the canonical matrix. The FastAPI service serves the static build and read-only artifacts on the ML VPS.', body_es: 'La construcción GPU o CPU crea evidencia canónica. La vía replay lee JSON versionado. La vía viva recalcula respuesta acotada en TypeScript para controles y animación. Es una aproximación visible y nunca reescribe la matriz canónica. FastAPI sirve build y artefactos en el VPS ML.' },
  { id: 'data', en: 'Data contract', es: 'Contrato de datos', svg: `${import.meta.env.BASE_URL}svg/tech/data.svg`, body_en: 'Contract 1 rejects missing, non-finite and physically inconsistent operating points, while flagging high-throughput or low-grade review states. Contract 2 binds each case to a manifest, byte count, schema, method coverage and artifact path. The browser types mirror the artifact shape. This keeps a visually rich product applicable to new data instead of turning it into a screenshot gallery.', body_es: 'El contrato 1 rechaza entradas faltantes, no finitas o físicamente inconsistentes, y marca puntos de revisión. El contrato 2 vincula caso, manifiesto, bytes, esquema, cobertura y ruta. Los tipos del navegador reflejan el artefacto. Así el producto visual sigue aplicable a datos nuevos y no es galería de capturas.' },
  { id: 'release', en: 'Release and deployment', es: 'Release y despliegue', svg: `${import.meta.env.BASE_URL}svg/tech/release.svg`, body_en: 'GitHub Actions builds the static Pages artifact and the ML VPS runs the same repository with a pinned Python environment behind nginx and TLS. Deployment does not train, benchmark or mutate scientific artifacts. Release verification checks health, title, manifest availability, case counts, static asset loading and representative interactions.', body_es: 'GitHub Actions construye el artefacto Pages y el VPS ML ejecuta el mismo repositorio con entorno Python fijado detrás de nginx y TLS. El despliegue no entrena, benchmarkea ni muta artefactos científicos. Verificación comprueba salud, título, manifiesto, conteos, assets e interacciones representativas.' },
] };
